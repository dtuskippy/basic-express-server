'use strict';

const { start } = require('./src/server');

start();



// /* index.js at the root of your repository, which will act as the “entry point” to your server. OK
// should require src/server.js OK
// should require dotenv, reading PORT from your .env file ?????
// It should call the .start() method from the server with the PORT set in your environment OK????
